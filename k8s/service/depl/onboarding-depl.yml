apiVersion: apps/v1
kind: Deployment
metadata:
  name: onboarding-service-depl
  namespace: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: onboarding-service-depl
  template:
    metadata:
      labels:
        app: onboarding-service-depl
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8080"
    spec:
      containers:
        - name: onboarding-container
          image: kwonhyeongil/onboarding-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
            - containerPort: 9090
          resources:
            limits:
              cpu: "1"
              memory: "500Mi"
            requests:
              cpu: "0.5"
              memory: "250Mi"
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: SPRING_DATA_REDIS_SENTINEL_MASTER
              value: "mymaster"
            - name: SPRING_DATA_REDIS_SENTINEL_NODES
              value: "redis.infra.svc.cluster.local:26379"
            - name: SPRING_DATA_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: omnimfa-secret
                  key: REDIS-PASSWORD
            - name: SPRING_DATA_REDIS_SENTINEL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: omnimfa-secret
                  key: REDIS-PASSWORD
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql-k8ssvc.infra.svc.cluster.local:3306/onboarding?useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: omnimfa-secret
                  key: MYSQL-PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: omnimfa-secret
                  key: JWT-SECRET
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: onboarding-k8ssvc
  namespace: service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: grpc
      port: 9090
      targetPort: 9090
  selector:
    app: onboarding-service-depl
